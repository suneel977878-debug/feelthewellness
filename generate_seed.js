const fs = require('fs');

// The easiest way to get the products is to read the local sqlite DB using prisma since we already seeded it earlier!
// Wait! I changed schema.prisma to use mysql!
// But dev.db is still there!
